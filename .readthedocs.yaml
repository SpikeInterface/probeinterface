version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.10"
  jobs:
    install:
      - pip install -U pip
      - pip install .
      - pip install --group docs

sphinx:
  configuration: doc/conf.py
